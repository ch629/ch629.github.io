baseURL = "https://charliehowes.com/"
languageCode = 'en-us'
title = "Charlie Howes | Contract Software Engineer"
theme = "terminal"
paginate = 5
enableRobotsTXT = true
enableGitInfo = true
disableKinds = ["taxonomy", "term"]
disablePathToLower = true

[sitemap]
  changefreq = "weekly"
  priority = 0.7
  filename = "sitemap.xml"

[author]
  name = "Charlie Howes"
  email = "charliehowes97@outlook.com"

[outputs]
  home = ["HTML"]
  section = ["HTML"]

[params]
  description = "Senior contract backend engineer specializing in Go, payments infrastructure, and distributed systems."
  subtitle = "Hire a senior Go backend contractor for payments and distributed systems delivery."
  keywords = "contract software engineer,go contractor,backend contractor,payments engineer,distributed systems,kafka,nats,uk,senior,senior engineer,finance,fintech"
  images = ["/img/og/charlie-howes-contract-engineer.svg"]

  # dir name of your main content (default is `content/posts`).
  # the list of set content will show up on your index page (baseurl).
  contentTypeName = "posts"

  # ["orange", "blue", "red", "green", "pink"]
  themeColor = "green"

  # if you set this to 0, only submenu trigger will be visible
  showMenuItems = 6

  # show selector to switch language
  showLanguageSelector = false

  # set theme to full screen width
  fullWidthTheme = false

  # center theme with default width
  centerTheme = false

  # set a custom favicon (default is a `themeColor` square)
  # favicon = "favicon.ico"

  # set post to show the last updated
  # If you use git, you can set `enableGitInfo` to `true` and then post will automatically get the last updated
  showLastUpdated = true
  # Provide a string as a prefix for the last update date. By default, it looks like this: 2020-xx-xx [Updated: 2020-xx-xx] :: Author
  # updatedDatePrefix = "Updated"

  # set all headings to their default size (depending on browser settings)
  # it's set to `true` by default
  # oneHeadingSize = false

[languages]
  [languages.en]
    languageName = "English"
    title = "Charlie Howes | Contract Software Engineer"
    subtitle = "Senior backend engineer for distributed systems and payments"
    owner = "Charlie Howes"
    keywords = "go,golang,payments,distributed-systems,kafka,nats,contractor,freelance,uk,remote"
    copyright = "© Charlie Howes"
    menuMore = "Show more"
    readMore = "Read more"
    readOtherPosts = "Read other posts"
    newerPosts = "Newer posts"
    olderPosts = "Older posts"
    missingContentMessage = "Page not found..."
    missingBackButtonLabel = "Back to home page"

    [languages.en.params.logo]
      logoText = "Charlie Howes"
      logoHomeLink = "/"

    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "about"
        name = "About"
        url = "/about"
      [[languages.en.menu.main]]
        identifier = "services"
        name = "Services"
        url = "/services"
      [[languages.en.menu.main]]
        identifier = "experience"
        name = "Experience"
        url = "/experience"
      [[languages.en.menu.main]]
        identifier = "cv"
        name = "CV"
        url = "/CharlieH_CV.pdf"
      [[languages.en.menu.main]]
        identifier = "contact"
        name = "Contact"
        url = "/contact"
      [[languages.en.menu.main]]
        identifier = "blog"
        name = "Blog"
        url = "/posts"
